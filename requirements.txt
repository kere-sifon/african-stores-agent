# Core LangChain packages
langchain>=1.3.4               # Core framework: chains, agents, prompts
langchain-ollama>=0.2.0        # Ollama integration (ChatOllama)
langchain-community>=0.4.2     # Community tools: DuckDuckGo, web loaders
langchain-aws>=1.5.0           # AWS Bedrock (ChatBedrockConverse)

# LangGraph — production agent orchestration (replaces langchain_classic AgentExecutor)
langgraph>=1.2.5               # StateGraph, ToolNode, conditional edges
langgraph-checkpoint-mongodb>=0.4.0  # MongoDB checkpointer for resumable runs

# AWS
boto3>=1.35.0

# Search (DuckDuckGo backend for langchain-community)
ddgs>=9.0.0

# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data & storage
pydantic>=2.0.0                # Structured output / data models
python-dotenv>=1.2.2
pymongo[srv]>=4.6.0            # MongoDB Atlas (SRV connection strings)

# HTML generation
jinja2>=3.1.0
