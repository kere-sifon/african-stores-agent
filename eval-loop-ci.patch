diff -ruN a/.github/workflows/crawl.yml b/.github/workflows/crawl.yml
--- a/.github/workflows/crawl.yml	2026-06-19 23:55:58.971342021 +0000
+++ b/.github/workflows/crawl.yml	2026-06-19 23:55:58.978590392 +0000
@@ -448,6 +448,20 @@
           NOTIFY_PASSWORD: ${{ secrets.NOTIFY_EMAIL_PASSWORD }}
           NOTIFY_TO:       ${{ secrets.NOTIFY_EMAIL_TO }}
 
+      # ── Upload eval report artifact ───────────────────────────────────────────
+      - name: Upload eval report artifact
+        if: >
+          always() &&
+          steps.mode.outputs.mode != 'smoke' &&
+          steps.mode.outputs.mode != 'generate' &&
+          steps.mode.outputs.mode != 'names'
+        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
+        with:
+          name: eval-report
+          path: eval_report.json
+          retention-days: 90
+          if-no-files-found: ignore
+
       # ── Upload site artifact ──────────────────────────────────────────────────
       - name: Upload site artifact
         if: always() && steps.mode.outputs.mode != 'smoke'
diff -ruN a/crawl_tracker.py b/crawl_tracker.py
--- a/crawl_tracker.py	2026-06-19 23:55:58.969422092 +0000
+++ b/crawl_tracker.py	2026-06-19 23:55:58.976541885 +0000
@@ -50,10 +50,17 @@
     cities_crawled: list[str],
     week_number: int,
     run_id: str = "local",
+    eval_results: list[dict] | None = None,
 ) -> None:
     """
     Upsert crawl record for this province+year+week.
     Sets last_crawled_at to datetime.utcnow().
+
+    If eval_results is provided (list of {"city", "category", "scores"} dicts
+    from eval_agents.evaluate_run()), an aggregated eval_summary is computed
+    and stored alongside the crawl record — this is what powers per-province
+    eval trend queries (e.g. "search precision over the last 10 crawls of
+    Ontario") without needing a separate eval datastore.
     """
     coll = _get_collection()
     if coll is None:
@@ -70,6 +77,12 @@
         "year": year,
         "run_id": run_id,
     }
+
+    if eval_results is not None:
+        from eval_agents import aggregate_run_evals
+
+        doc["eval_summary"] = aggregate_run_evals(eval_results)
+
     try:
         coll.update_one(
             {"province": province, "year": year, "week_number": week_number},
@@ -137,6 +150,31 @@
     return results
 
 
+def get_eval_trend(province: str | None = None, limit: int = 20) -> list[dict]:
+    """
+    Return recent crawl records with eval_summary populated, most recent first.
+    Pass province to filter to a single province; omit for all provinces
+    (useful for an "eval trend over time" chart on the ops dashboard).
+
+    Only records that actually have an eval_summary are returned — older
+    crawl_history records predating this feature won't have one.
+    """
+    coll = _get_collection()
+    if coll is None:
+        return []
+
+    query: dict = {"eval_summary": {"$exists": True}}
+    if province:
+        query["province"] = province
+
+    try:
+        cursor = coll.find(query, sort=[("last_crawled_at", -1)], limit=limit)
+        return list(cursor)
+    except PyMongoError as e:
+        print(f"  [crawl_tracker] Warning: failed to read eval trend ({e})")
+        return []
+
+
 def was_crawled_this_week(province: str) -> bool:
     """Return True if province was crawled in the current ISO week and year."""
     coll = _get_collection()
diff -ruN a/eval_agents.py b/eval_agents.py
--- a/eval_agents.py	2026-06-19 23:55:58.965950832 +0000
+++ b/eval_agents.py	2026-06-19 23:55:58.973139242 +0000
@@ -212,6 +212,167 @@
     }
 
 
+def aggregate_run_evals(eval_results: list[dict]) -> dict:
+    """
+    Combine per-(city, category) eval_run() outputs from a multi-city crawl
+    (province, province-weekly, agent-full) into a single run-level summary.
+
+    Args:
+        eval_results: list of dicts, each shaped like:
+            {"city": str, "category": str, "scores": <evaluate_run() output>}
+
+    Returns:
+        dict with keys: counts, averages, worst, totals
+            counts    — how many (city, category) runs were evaluated
+            averages  — mean of each per-agent metric across all runs
+            worst     — the single worst (city, category) per metric, useful
+                        for spotting where a crawl regressed
+            totals    — summed total_saved / total_errors across the run
+    """
+    if not eval_results:
+        return {
+            "counts": 0,
+            "averages": {
+                "search_precision": 0.0,
+                "validator_accuracy": 0.0,
+                "storage_new_insert_rate": 0.0,
+            },
+            "worst": {},
+            "totals": {"total_saved": 0, "total_errors": 0},
+        }
+
+    metrics = ["search_precision", "validator_accuracy", "storage_new_insert_rate"]
+    sums = dict.fromkeys(metrics, 0.0)
+    worst: dict[str, dict] = {}
+    total_saved = 0
+    total_errors = 0
+
+    for entry in eval_results:
+        summary = entry.get("scores", {}).get("summary", {})
+        label = f"{entry.get('city', '?')} / {entry.get('category', '?')}"
+
+        for m in metrics:
+            val = summary.get(m, 0.0)
+            sums[m] += val
+            if m not in worst or val < worst[m]["score"]:
+                worst[m] = {"score": val, "where": label}
+
+        total_saved += summary.get("total_saved", 0)
+        total_errors += summary.get("total_errors", 0)
+
+    n = len(eval_results)
+    averages = {m: round(sums[m] / n, 3) for m in metrics}
+
+    return {
+        "counts": n,
+        "averages": averages,
+        "worst": worst,
+        "totals": {"total_saved": total_saved, "total_errors": total_errors},
+    }
+
+
+def print_run_summary(aggregate: dict, run_label: str = "Crawl run") -> None:
+    """Human-readable run-level eval summary for CI logs and step summaries."""
+    print("\n" + "=" * 60)
+    print(f"  EVAL SUMMARY — {run_label}")
+    print("=" * 60)
+    print(f"  (city, category) runs evaluated: {aggregate.get('counts', 0)}")
+
+    avg = aggregate.get("averages", {})
+    worst = aggregate.get("worst", {})
+    print(f"\n  Avg Search Precision        {avg.get('search_precision', 0):.3f}", end="")
+    if "search_precision" in worst:
+        w = worst["search_precision"]
+        print(f"   (worst: {w['score']:.3f} @ {w['where']})")
+    else:
+        print()
+
+    print(f"  Avg Validator Accuracy      {avg.get('validator_accuracy', 0):.3f}", end="")
+    if "validator_accuracy" in worst:
+        w = worst["validator_accuracy"]
+        print(f"   (worst: {w['score']:.3f} @ {w['where']})")
+    else:
+        print()
+
+    print(f"  Avg Storage New-Insert Rate {avg.get('storage_new_insert_rate', 0):.3f}", end="")
+    if "storage_new_insert_rate" in worst:
+        w = worst["storage_new_insert_rate"]
+        print(f"   (worst: {w['score']:.3f} @ {w['where']})")
+    else:
+        print()
+
+    totals = aggregate.get("totals", {})
+    print(f"\n  Total Stores Saved   {totals.get('total_saved', 0)}")
+    print(f"  Total Pipeline Errors {totals.get('total_errors', 0)}")
+    print("=" * 60)
+
+
+def write_eval_report_json(
+    aggregate: dict,
+    eval_results: list[dict],
+    run_label: str,
+    path: str = "eval_report.json",
+) -> None:
+    """
+    Write the full eval report (aggregate + per-city detail) to a JSON file
+    for upload as a CI artifact and for trend analysis over time.
+    """
+    from datetime import UTC, datetime
+
+    report = {
+        "run_label": run_label,
+        "generated_at": datetime.now(UTC).isoformat(),
+        "aggregate": aggregate,
+        "per_run": eval_results,
+    }
+    with open(path, "w") as f:
+        json.dump(report, f, indent=2, default=str)
+    logger.info("Eval report written to %s", path)
+
+
+def write_step_summary_markdown(aggregate: dict, run_label: str) -> None:
+    """
+    Append a markdown eval summary to GITHUB_STEP_SUMMARY, which GitHub
+    Actions renders directly in the workflow run UI. No-op outside CI
+    (when the env var isn't set).
+    """
+    import os
+
+    summary_path = os.environ.get("GITHUB_STEP_SUMMARY")
+    if not summary_path:
+        return
+
+    avg = aggregate.get("averages", {})
+    worst = aggregate.get("worst", {})
+    totals = aggregate.get("totals", {})
+
+    def worst_note(metric: str) -> str:
+        w = worst.get(metric)
+        return f"worst: {w['score']:.3f} @ {w['where']}" if w else "—"
+
+    def metric_row(label: str, key: str) -> str:
+        score = avg.get(key, 0)
+        return f"| {label} | {score:.3f} | {worst_note(key)} |"
+
+    lines = [
+        f"## 📊 Per-Agent Eval Report — {run_label}",
+        "",
+        f"_{aggregate.get('counts', 0)} (city, category) runs evaluated_",
+        "",
+        "| Metric | Average | Notes |",
+        "|---|---|---|",
+        metric_row("Search Precision", "search_precision"),
+        metric_row("Validator Accuracy", "validator_accuracy"),
+        metric_row("Storage New-Insert Rate", "storage_new_insert_rate"),
+        "",
+        f"**Total stores saved:** {totals.get('total_saved', 0)}  ",
+        f"**Total pipeline errors:** {totals.get('total_errors', 0)}",
+        "",
+    ]
+    with open(summary_path, "a") as f:
+        f.write("\n".join(lines) + "\n")
+
+
 def print_eval_report(scores: dict) -> None:
     """Human-readable evaluation report for logs and interview demos."""
     s = scores.get("summary", {})
diff -ruN a/run.py b/run.py
--- a/run.py	2026-06-19 23:55:58.967698656 +0000
+++ b/run.py	2026-06-19 23:55:58.975225550 +0000
@@ -136,14 +136,30 @@
 def run_city_crawl(city: str, use_agent: bool) -> None:
     if use_agent:
         from config import SEARCH_QUERIES
+        from eval_agents import (
+            aggregate_run_evals,
+            evaluate_run,
+            print_run_summary,
+            write_eval_report_json,
+            write_step_summary_markdown,
+        )
         from supervisor import build_supervisor_agent, run_supervisor_for_city
 
         app = build_supervisor_agent()
+        eval_results: list[dict] = []
         for category in SEARCH_QUERIES:
             try:
-                run_supervisor_for_city(app, city, category)
+                result = run_supervisor_for_city(app, city, category)
+                scores = evaluate_run(result)
+                eval_results.append({"city": city, "category": category, "scores": scores})
             except Exception as e:
                 print(f"  [supervisor] Error on ({city}, {category}): {e} — continuing...")
+
+        aggregate = aggregate_run_evals(eval_results)
+        run_label = f"City crawl — {city}"
+        print_run_summary(aggregate, run_label)
+        write_eval_report_json(aggregate, eval_results, run_label)
+        write_step_summary_markdown(aggregate, run_label)
     else:
         from pipeline import run_city_pipeline
 
@@ -155,6 +171,13 @@
     """Crawl all cities in a province using the multi-agent supervisor."""
     from config import SEARCH_QUERIES
     from crawl_tracker import record_province_crawl
+    from eval_agents import (
+        aggregate_run_evals,
+        evaluate_run,
+        print_run_summary,
+        write_eval_report_json,
+        write_step_summary_markdown,
+    )
     from provinces import PROVINCE_CITIES, get_cities_for_province
     from supervisor import build_supervisor_agent, run_supervisor_for_city
 
@@ -172,22 +195,31 @@
     print(f"\n🏙️  Province crawl: {canonical} ({len(cities)} cities) [supervisor]\n")
 
     app = build_supervisor_agent()
+    eval_results: list[dict] = []
     for city in cities:
         for category in SEARCH_QUERIES:
             try:
-                run_supervisor_for_city(app, city, category)
+                result = run_supervisor_for_city(app, city, category)
+                scores = evaluate_run(result)
+                eval_results.append({"city": city, "category": category, "scores": scores})
             except Exception as e:
                 print(f"  [supervisor] Error on ({city}, {category}): {e} — continuing...")
 
     after = get_stats()["total"]
     saved = after - before
     week = datetime.now().isocalendar()[1]
-    record_province_crawl(canonical, saved, cities, week, run_id)
+    record_province_crawl(canonical, saved, cities, week, run_id, eval_results=eval_results)
 
     print(f"\n[crawl] Province crawl complete: {canonical}")
     print(f"[crawl] New stores saved: {saved}")
     print(f"[crawl] Total in database: {after}")
 
+    aggregate = aggregate_run_evals(eval_results)
+    run_label = f"Province crawl — {canonical}"
+    print_run_summary(aggregate, run_label)
+    write_eval_report_json(aggregate, eval_results, run_label)
+    write_step_summary_markdown(aggregate, run_label)
+
     print_stats()
     generate()
 
@@ -263,23 +295,46 @@
 
 def run_agent_test(store_names: list[str] | None, city: str) -> None:
     from config import llm_config_summary
+    from eval_agents import (
+        aggregate_run_evals,
+        evaluate_run,
+        print_run_summary,
+        write_eval_report_json,
+        write_step_summary_markdown,
+    )
     from supervisor import build_supervisor_agent, run_supervisor_for_city
 
     init_db()
     app = build_supervisor_agent()
+    eval_results: list[dict] = []
 
     if store_names:
         print(f"🤖 Supervisor — named stores in {city}")
         print(f"   LLM: {llm_config_summary()}\n")
         for name in store_names:
             try:
-                run_supervisor_for_city(app, city, name)
+                result = run_supervisor_for_city(app, city, name)
+                eval_results.append(
+                    {"city": city, "category": name, "scores": evaluate_run(result)}
+                )
             except Exception as e:
                 print(f"  [supervisor] Error on {name}: {e} — continuing...")
     else:
         print("🤖 Supervisor agent test: African grocery stores in Toronto")
         print(f"   LLM: {llm_config_summary()}\n")
-        run_supervisor_for_city(app, city, "African grocery store")
+        result = run_supervisor_for_city(app, city, "African grocery store")
+        eval_results.append(
+            {
+                "city": city,
+                "category": "African grocery store",
+                "scores": evaluate_run(result),
+            }
+        )
+
+    aggregate = aggregate_run_evals(eval_results)
+    print_run_summary(aggregate, "Agent test")
+    write_eval_report_json(aggregate, eval_results, "Agent test")
+    write_step_summary_markdown(aggregate, "Agent test")
 
     print_stats()
     generate()
@@ -287,20 +342,38 @@
 
 def run_agent_full():
     from config import SEARCH_QUERIES, TARGET_CITIES
+    from eval_agents import (
+        aggregate_run_evals,
+        evaluate_run,
+        print_run_summary,
+        write_eval_report_json,
+        write_step_summary_markdown,
+    )
     from supervisor import build_supervisor_agent, run_supervisor_for_city
 
     init_db()
     app = build_supervisor_agent()
     total = len(TARGET_CITIES) * len(SEARCH_QUERIES)
     completed = 0
+    eval_results: list[dict] = []
     for city in TARGET_CITIES:
         for category in SEARCH_QUERIES:
             completed += 1
             print(f"\n[{completed}/{total}] city={city} | category={category}")
             try:
-                run_supervisor_for_city(app, city, category)
+                result = run_supervisor_for_city(app, city, category)
+                eval_results.append(
+                    {"city": city, "category": category, "scores": evaluate_run(result)}
+                )
             except Exception as e:
                 print(f"  [supervisor] Error on ({city}, {category}): {e} \u2014 continuing...")
+
+    aggregate = aggregate_run_evals(eval_results)
+    run_label = "Full crawl — all cities"
+    print_run_summary(aggregate, run_label)
+    write_eval_report_json(aggregate, eval_results, run_label)
+    write_step_summary_markdown(aggregate, run_label)
+
     generate()